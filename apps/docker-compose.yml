version: '2'

services:
    drone:
      build: drone
      env_file:
        - ./dronerc.env
      ports:
        - 80:80
      volumes:
        - /data/drone:/var/lib/drone
        - /var/run/docker:/var/run/docker



